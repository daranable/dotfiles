" see also ~/.vim/ftplugin/typescript.vim

" always show the sign column
" this prevents flicker when nvim-typescript refreshes the signs
set signcolumn=yes

" enable omni completion via nvim-typescript
set omnifunc=TSOmnicFunc
